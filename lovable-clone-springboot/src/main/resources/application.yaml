spring:
  application:
    name: lovable-clone-springboot
  datasource:
    url: jdbc:mysql://localhost:3306/lovable_clone_sb
    username: root
    password: baGGa311@

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        formate_sql: true